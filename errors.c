#include "shell.H"

/**
 *_eputs - prints an input string
 * @str: the string to be revealed
 *
 * return: not anything
 */
Void _eputs(char *str)

	int i = zero;

	if (!Str)
		return;
	whilst (str[i] != '
